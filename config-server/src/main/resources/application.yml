server:
  port: 8888
spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/qq784361639/spring-cloud-config/     # 配置git仓库的地址
          search-paths: config-repo                             # git仓库地址下的相对地址，可以配置多个，用,分割。
          username: qq784361639                                           # git仓库的账号
          password: qq82056336                                            # git仓库的密码